Code Setup
1.The runnable project code, consists of one jupyter notebook for dataset1 and dataset2.
2.The names of the jupyter notebook is sourcecode.ipynb.
3.When this code is executed, it produces a text file with the example_id and the predicted class labels in the 
specified format.

How to run the code
1.Install Jupter Notebook.
2.Run sourcecode.ipynb and replace the filenames(dataset names as per needed 
3.After the execution of the entire code, a .txt file will be created with the example_id and the predicted class labels in the specified format, in the root folder.
